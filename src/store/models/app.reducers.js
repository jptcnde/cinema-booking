// eslint-disable-next-line import/prefer-default-export
export const setFetching = (state, fetching) => ({
  ...state,
  fetching
});

export const setCurrentPatron = (state, currentPatron) => ({
  ...state,
  currentPatron
});
